ja:
  activerecord:
    errors:
      messages:
        record_invalid: "バリデーションに失敗しました: %{errors}"
        restrict_dependet_destroy:
          has_one: "%{record}が存在しているので削除できません"
          has_many: "%{record}が存在しているので削除できません"
    models:
      road: ルート
      like: いいね
    attributes:
      road:
        title: タイトル
        description: 説明文
        user: ユーザー
      like:
        user: ユーザー
        road: 投稿
  time:
    formats:
      default: "%Y/%m/%d"
  views:
    pagination:
      first: "&laquo; 最初"
      last: "最後 &raquo;"
      previous: "&lsaquo; 前"
      next: "次 &rsaquo;"
      truncate: "..."