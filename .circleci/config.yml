version: 2.1
jobs:
  deploy:
    machine:
      enabled: true
    steps:
      - add_ssh_keys:
          fingerprints:
            - "69:04:35:b2:28:2b:11:1a:18:72:df:2a:c2:1b:e7:73"
      - run: ssh -p $SSH_PORT $SSH_USER@$SSH_HOST "/var/www/Tamari-Ba/deploy.sh"
 
workflows:
  version: 2
  deploy:
    jobs:
      - deploy:
          filters:
            branches:
              only: develop