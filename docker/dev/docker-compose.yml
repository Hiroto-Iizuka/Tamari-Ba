version: '2'
services:
  app:
    container_name: tamari-ba-app
    build: ./app/
    image: tamari-ba-app
    ports:
      - '80:80'
    volumes:
      - ~/git/github/Tamari-Ba:/var/www/app/
    tty: true
    depends_on:
      - db
  db:
    container_name: tamariba
    build: ./mysql/
    image: tamariba
    ports:
      - '3306:3306'
    environment:
      MYSQL_DATABASE: tamari_ba_dev
      MYSQL_ROOT_PASSWORD: password
      TZ: "Asia/Tokyo"
    volumes:
      - ./mysql/db_data:/var/lib/mysql
